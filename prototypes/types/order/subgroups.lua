data:extend({
  {
    type="item-subgroup",
    name="a",
    group="substances"
  }
})
-- for name, _ in pairs(Solid_substances) do
--   local subgroup = {
--     type = "item-subgroup",
--     name = name,
--     group = "substances"
--   }
--   data:extend({ subgroup })
-- end